<Window x:Class="VehiculoCatalogoCrud.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModel="clr-namespace:VehiculoCatalogoCrud.ViewModel"
        xmlns:Core="clr-namespace:VehiculoCatalogoCrud.Core"
        xmlns:local="clr-namespace:VehiculoCatalogoCrud"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Background="#36393e">
        </Border>

        <ComboBox Height="20" Width="70" ItemsSource="{Binding  }" x:Name="cbFabricantes">
            
        </ComboBox>
        
    </Grid>
</Window>
